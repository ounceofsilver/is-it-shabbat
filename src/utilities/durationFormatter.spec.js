import {
	underAWeek,
	formatHolidayDuration,
} from './durationFormatter';

describe('durationFormatter:', () => {
	describe('underAWeek', () => {
		it('should work', () => {
			// TODO(jessica.fulford): complete this
			expect(underAWeek).to.be.a('function');
		});
	});

	describe('formatHolidayDuration', () => {
		it('should work', () => {
			// TODO(jessica.fulford): complete this
			expect(formatHolidayDuration).to.be.a('function');
		});
	});
});
